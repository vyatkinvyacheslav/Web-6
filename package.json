{
  "name": "web-6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "chrome": "cypress run --browser chrome --headed --config-file cypress_2.config.js",
    "chrome": "cypress run --browser chrome --headless --config-file cypress_2.config.js",
    "electron": "cypress run --browser electron --headed --config-file cypress_2.config.js",
    "electron": "cypress run --browser electron --headless --config-file cypress_2.config.js"
    },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.12.0"
  }
}
